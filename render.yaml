services:
  - type: web
    name: garden-train-server
    runtime: python
    plan: free
    rootDir: garden-train-server
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
